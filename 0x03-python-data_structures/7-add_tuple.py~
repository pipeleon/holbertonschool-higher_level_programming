#!/usr/bin/python3
def add_tuple(tuple_a=(), tuple_b=()):
    new = []
    for i in range(2):
        if (len(tuple_a) >= 2 and len(tuple_b) >= 2):
            new.append(tuple_a[i] + tuple_b[i])
    return tuple(new)
